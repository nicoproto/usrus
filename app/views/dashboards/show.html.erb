<h1>My list of item</h1>
<% @items.each do |item| %>
  <h2><%= item.name %></h2>
  <p><%= item.address %></p>
  <%= link_to "Delete", item_path(item), method: :delete, class: "btn btn-primary" %>
  <%= link_to "Update", edit_item_path(item), class: "btn btn-secondary" %>

<% end %>

