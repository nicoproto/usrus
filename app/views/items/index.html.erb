<div class="margin">
  <%= render 'shared/filterbar' %>
  <div class="d-flex">
    <div class="cards" id="cards-container">
      <%= render 'items_partial', items: @items %>
    </div>
    <div
      id="map"
      class=""
      style="width: 65%; height: 100vh"
      data-markers="<%= @markers.to_json %>"
      data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
    </div>
  </div>
</div>
